<div class="header-nav">
  <% if logged_in? && current_user.admin? %>
    <ul>

      <li>
        <%= link_to 'new location', new_category_location_path(@category.slug),
                    class: 'waves-effect waves-light btn white' %>
      </li>
      <li>

      </li>
    <% else %>
    <li>
      <%= link_to "back", :back, class: 'waves-effect waves-light btn white' %>
    </li>
  <% end %>

  </ul>
</div>
<div class="row">
  <div class="col l12 m12 s12">
    <br/>
    <% if logged_in? && current_user.admin? %>
        <span>New Section &nbsp </span>
      <% if @location %>
        <%= link_to new_location_section_path(@location.id), class: 'location-button btn-floating btn-small waves-effect waves-light blue' do %>
      <% end %>
  <i class="location-icon material-icons">+</i>
    <% end %>
    <% end %>
  <h3>
    Where do you usually park?
  </h3><%= render 'shared/location_feed' %>
  </div>
</div>