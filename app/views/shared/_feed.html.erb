<ol class="microposts">
  <% if logged_in? && current_user.admin? %>
      <li>New Workplace &nbsp; <span>
      <a href="/workplaces/new" class="location-button btn-floating btn-small waves-effect waves-light blue">
      <i class="location-icon material-icons">+</i></a>
    </span>
      </li>
    <% if @feed_items.any? %>
      <%= render @feed_items %>
      <hr />
      </ol>
  <% end %>

    <% else %>

<% end %>