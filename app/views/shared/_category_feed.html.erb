<% if logged_in? && current_user.admin? %>
  <ol class="microposts">
    <% if @category_feed.any? %>
        <%= render @category_feed%>
            <%= will_paginate @category_feed %>
    <% end %>
  </ol>
<% end %>