<div class="header-nav">
  <% if logged_in? && current_user.admin? %>
    <ul>

      <li>
        <%= link_to 'new section', new_location_section_path(@location.slug),
                    class: 'waves-effect waves-light btn white' %>
      </li>
      <li>

      </li>
  <% else %>
    <li>
      <%= link_to "back", :back, class: 'waves-effect waves-light btn white' %>
    </li>
  <% end %>

  </ul>
</div>
<div class="row">
  <div class="col l12 m12 s12">
    <br/>
    <div class="center-items">
      <%= react_component('App',  prerender: false) %>

    </div>
    <!-- render 'shared/location_feed'  -->
  </div>
</div>