<section class="row">
  <div class="col l8 m8 s12 offset-l2">
    <h3><%= @workplace.workplace_name %> </h3>
    <h5>Information</h5>
    <h5>Locations <%= link_to "View locations", workplace_locations_path(@workplace.slug) %></h5>
    <h5><%= link_to "Create New Location", new_workplace_location_path(@workplace.slug) %></h5>
  </div>
</section>