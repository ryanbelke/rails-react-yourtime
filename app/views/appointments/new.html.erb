<div class="header-nav">
  <% if logged_in? && current_user.admin? %>
    <ul>
      <li>
        <h6>
        </h6>
      </li>
      <li>
      </li>
      <li>
      </li>
  <% end %>
  <li>
    <%= link_to "back", :back, class: 'waves-effect waves-light btn white' %>
  </li>
  </ul>
</div>

<% if request.original_url.to_s.include? 'service' %>
  <div class="row">
    <div class="col l3 m6 s12 offset-l1">
      <div class="paper-no-border">
        <div class="css-flash">
          <div class="icon-div">
            <i class="fa fa-calendar" aria-hidden="true"></i>
          </div>
          <div class="flash-text">
            <h6>Appointment Date</h6>
          </div>
        </div>
        <div class="date-selector">
          <label>Select a date</label>
          <input class="datepicker", placeholder='Date' />
        </div>
      </div>
    </div>
    <div class="col l6 m6 s12">
      <div class="paper-no-border">
        <div class="css-flash">
          <div class="icon-div-info">
            <i class="fa fa-info" aria-hidden="true"></i>
          </div>
          <div class="flash-text">
            <h6>Appointment Info:</h6>
          </div>
        </div>
        <div class="form-info">
          <span class="form-header">Category:</span>
          <span class="form-text">&nbsp; <%= @service.category.category_name %></span>
          <br />
          <span class="form-header">Service: </span>
          <span class="form-text">&nbsp;&nbsp;&nbsp; &nbsp; <%= @service.service_name %></span>
          <br />
          <span class="form-header">Price: </span>
          <span class="form-text">&nbsp;&nbsp; &nbsp; &nbsp; &nbsp; $<%= @service.service_price %></span>
          <br />
          <span class="form-header">Vendor: </span>
          <span class="form-text"> &nbsp;&nbsp;&nbsp; &nbsp;<%= @service.service_vendor %></span>
          <br />
          <span class="form-header">Time Frame: </span>
          <span class="form-text">
            <% if @service.service_time_to_complete < 8 %>
             <%= 'Same Day' %>
            <% else %>
             <%= 'Extended completion time' %>
            <% end %>
          </span>
        </div>
      </div>
      <br />
      <div class="paper-no-border">
        <div class="css-flash-warning">
          <div class="icon-div-warning">
            <i class="fa fa-user" aria-hidden="true"></i>
          </div>
          <div class="flash-text">
            <h6>User Info</h6>
          </div>
        </div>
      <div class="center-align" >
        <br />

        <h6><%= link_to 'Please sign in or register here', login_path %></h6>
        <br />
        <small>Don't worry, we will save this page and bring you back once you register</small>
        <br />        <br />

      </div>

      </div>

    </div>

  </div>

  <script>

      $('.datepicker').pickadate({
          selectMonths: true, // Creates a dropdown to control month
          selectYears: 15, // Creates a dropdown of 15 years to control year,
          today: 'Today',
          clear: 'Clear',
          close: 'Ok',
          closeOnSelect: false, // Close upon selecting a date,
          disable: [
              true,
              <% @dates.each do |a| %>
              <%= a.to_s.gsub('"', '') + ',' %>
              <% end %>
          ]
      });
  </script>
<% else %>
  <div class="row">
    <div class="col l12 m6 s12">
      <%= render 'shared/services_feed' %>
    </div>
  </div>
<% end %>